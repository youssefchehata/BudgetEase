name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.14.0]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies (Frontend)
      run: |
        cd frontend  # Navigate to the frontend directory
        npm ci       # Install dependencies using the package-lock.json located in the frontend directory

    - name: Build frontend (Vite)
      run: |
        cd frontend  # Navigate to the frontend directory again
        npm run build # Run the build command for frontend app

    - name: Deploy with gh-pages
      run: |
        cd frontend  # Ensure we're in the frontend directory
        git remote set-url origin https://git:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git
        npm run deploy -- -u "github-actions-bot <support+actions@github.com>"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
